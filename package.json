{
	"name": "superpowers",
	"displayName": "superpowers",
	"description": "Edit your document with JavaScript functions",
	"version": "0.0.1",
	"publisher": "thykka",
	"engines": {
		"vscode": "^1.30.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:extension.superpowers",
		"onCommand:extension.superpresets",
		"onCommand:extension.supersorts"
	],
	"main": "./extension.js",
	"contributes": {
		"configuration": {
			"title": "Superpowers",
			"properties": {
				"superpowers.mapPresets": {
					"type": "array",
					"items": {
						"type": "object",
						"title": "Custom map presets",
						"description": "Custom map presets",
						"properties": {
							"name": {
								"type": "string",
								"description": "Map preset name"
							},
							"function": {
								"type": "string",
								"description": "Map preset function"
							}
						}
					},
					"default": [
						{
							"name": "append difference between times",
							"function": "(v) => { var times = v.match(/[0-9]+:[0-9]+/g).map(t => { let parts = t.split(':'); return parseInt(parts[0]) + (parts[1] / 60) }); return v + ' (' + (times[1] - times[0]) + 'h)'; }"
						},
						{
							"name": "convert UPPERCASE",
							"function": "(v) => v.toUpperCase()"
						},
						{
							"name": "convert lowercase",
							"function": "(v) => v.toLowerCase()"
						},
						{
							"name": "convert camelCase",
							"function": "(v) => v.trim().replace(/(?:^|\\s|_|-+)(\\w)/g, (_, c, o) => o !== 0 ? c.toUpperCase() : c)"
						},
						{
							"name": "insert current time (rounded)",
							"function": "() => { let now = new Date( Math.round(Date.now() / 1000 / 60 / 15) * 1000 * 60 * 15 ); return now.getHours() + ':' + ('0' + now.getMinutes()).substr(-2) }"
						},
						{
							"name": "prefix index",
							"function": "(v, i) => `${i+1}. ${v}`"
						}
					]
				},
				"superpowers.sortPresets": {
					"type": "array",
					"items": {
						"type": "object",
						"title": "Custom sort presets",
						"description": "Custom sort presets",
						"properties": {
							"name": {
								"type": "string",
								"description": "Sort preset name"
							},
							"function": {
								"type": "string",
								"description": "Sort preset function"
							}
						}
					},
					"default": [
						{ "name": "natural (alphanumeric)", "function": "(s1, s2) => { const a = s1.split(/(\\d+|\\D+)/).filter(function(s){return s!=''}); const b = s2.split(/(\\d+|\\D+)/).filter(function(s){return s!=''}); let cmp = 0; for (let i = 0; 0 == cmp && i < a.length && i < b.length; i++) { let n1 = a[i] - 0, n2 = b[i] - 0; if (!isNaN(n1) && !isNaN(n2)) cmp = n1 - n2; else if (a[i] < b[i]) cmp = -1; else if (a[i] > b[i]) cmp = 1; } return cmp; }"}
					]
				}
			}
		},
		"commands": [
			{
				"command": "extension.superpowers",
				"title": "Superpowers (custom map function)"
			}, {
				"command": "extension.superpresets",
				"title": "Superpresets (preset map functions)"
			}, {
				"command": "extension.supersorts",
				"title": "Supersorts (preset sorting functions)"
			}
		]
	},
	"scripts": {
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "node ./node_modules/vscode/bin/test"
	},
	"devDependencies": {
		"typescript": "^3.1.4",
		"vscode": "^1.1.25",
		"eslint": "^4.11.0",
		"@types/node": "^8.10.25",
		"@types/mocha": "^2.2.42"
	},
	"repository": {
		"type": "git",
		"url": "git://bitbucket.org/thykka/vscode-superpowers.git"
	}
}
